LIFEMATE AI V5 — START HERE

1. ZIP extract karo. LifeMateAI-main folder kholo.
2. Is folder KE ANDAR ke files/folders apne existing GitHub repository ke root mein replace karo. Outer folder ko nested upload mat karo.
3. New files bhi include karo: ProductivityScreens.kt, domain/Productivity.kt, app/src/test/.
4. GitHub Actions: Build Android APK and AAB -> Run workflow.
5. Green build ke baad LifeMateAI-debug-APK artifact download/extract karke APK install karo.
6. Play Store ke liye existing signing key ke saath signed-release workflow use karo; unsigned AAB upload mat karo.

Version: 5.0.0 / code 5. Same package ID. Existing app update ke liye same signing key zaroori hai.
Uninstall karne se local data delete ho sakta hai. Debug aur release apps ke package IDs alag hain.

NEW: redesigned teal dashboard, Focus Studio (15/25/50 minutes), weekly insights,
recorded habit history, task search/edit/overdue filter/priority sort, safer deletion,
auto-scrolling chat, improved small-screen dialogs and reminder settings fixes.

AI backend pehle se connected hai to settings preserve hongi. Nahi connected hai to
AI_SETUP_EASY.md dekho. No API key or live backend has been added by this update.

VALIDATION: Source and ZIP checks performed. Android build, on-device layout and
JUnit execution require the included GitHub workflow / Android Studio; they were
not run in the editing environment. Focus has no background alarm.
